import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 15,
  },
  topContent: {
    marginVertical: 10,
  },
  customerText: {
    color: 'white',
  },
});

export default styles;
